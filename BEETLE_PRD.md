# Beetle 가계부 프로젝트 기획 및 설계 문서 (PRD)

## 1. 프로젝트 개요
- **프로젝트 이름:** Beetle (모노레포 루트)
- **목적:** 엑셀이나 범용 템플릿의 한계를 벗어나, 개인 맞춤형으로 소비 패턴을 분석하고 관리할 수 있는 프라이빗 가계부 시스템 구축 (백엔드 서버 + 웹 클라이언트)
- **기술 스택:**
  - **백엔드:** Kotlin, Spring Boot (4.x), Spring Data JPA, MySQL, Flyway, Gradle, Docker
  - **프론트엔드:** React, Vite, TypeScript, Tailwind CSS, shadcn/ui, TanStack Query, Zustand, Recharts
- **아키텍처:** 모노레포 구조 내에서 백엔드는 클린 아키텍처(Clean Architecture) 및 도메인 주도 설계(DDD) 기반의 엄격한 의존성 역전 원칙(DIP) 적용. 프론트엔드는 기능별 모듈(Feature-based) SPA 구조

> **도메인 계산의 단일 출처:** 청구일 산출, 할부 금액 분할 같은 도메인 계산은 **백엔드만** 수행합니다.
> 프론트엔드는 서버가 계산한 값을 표시만 합니다. 같은 규칙이 두 곳에 있으면 반드시 어긋납니다.

---

## 2. 핵심 도메인 요구사항 및 설계

네 개의 도메인은 각각 독립 애그리거트이며, **애그리거트 간 참조는 식별자(ID)로만** 합니다.

### ① 거래 내역 (Transaction)
- **속성:** ID, 카테고리, 결제 수단, 금액, 메모(사용처), 소비일(`spentDate`), 청구일(`billDate`), 결제 완료 여부(`isSettled`), 통계 제외 여부(`isExcludedFromStats`), 할부 계획 참조(`installmentPlanId`, `installmentSequence`)
- **특징:**
  - 실제 카드를 긁은 날짜(**소비일**)와 통장에서 돈이 실제로 빠져나가는 날짜(**청구일**)를 분리하여 관리합니다.
  - 소비 패턴 분석은 **소비일** 기준, 현금 흐름 및 자금 통제는 **청구일** 기준으로 집계합니다.
  - 조회·통계 API는 `basis` 파라미터(`SPENT` / `BILL`)로 두 집계 축을 전환합니다.
- **통계 제외 (`isExcludedFromStats`):**
  - 회사가 전액 지원하는 통신비처럼 **기록은 남기되 실지출이 없는 항목**을 모든 집계에서 제외합니다.
  - **카테고리 단위가 아니라 거래 단위로 판단합니다.** 같은 카테고리에서도 자부담분이 있는 거래와 없는 거래가 섞이기 때문입니다.
- **불변식:**
  - 금액은 0원보다 큽니다. 0원 거래는 기록할 의미가 없습니다.
  - **청구일은 소비일보다 앞설 수 없습니다.**
  - 메모는 200자 이하입니다.
  - 할부 계획 식별자와 회차 번호는 함께 존재하거나 함께 없습니다.
- **결제 완료 여부(`isSettled`)의 기본값:** 거래 등록 시 지정하지 않으면 **결제 수단에서 도출**합니다.
  - 즉시 결제 수단(현금·체크카드·계좌)은 소비 시점에 이미 출금이 끝났으므로 **`true`**
  - 신용카드는 청구일에 출금되므로 **`false`** (사용자가 청구일 이후 직접 정산 표시)
  - 미래 날짜의 계좌 자동이체처럼 예외가 필요하면 명시적으로 지정합니다.
    `false` 를 명시하면 "미정산" 을 지정한 것으로 해석되어 도출이 일어나지 않습니다.

  > 이 도출이 없으면 **이미 나간 현금이 "아직 출금되지 않은 청구 예정액" 에 잡혀**
  > 현금 흐름 예측이 틀어집니다.
- **상태 전이 제약:** 결제(출금) 완료된 거래는 **금액 정정과 일자 변경을 거부**합니다. 이미 통장에서 나간 돈의 기록이므로 사후에 바꿀 수 없습니다. 잘못 표시했다면 완료를 먼저 되돌립니다.

### ② 카테고리 (Category)
- **속성:** ID, 이름, 타입(수입 `INCOME`, 지출 `EXPENSE`, 이체 `TRANSFER`), 성격(`ExpenseNature`)
- **지출 성격 분리:**
  - **고정비 (`FIXED`):** 월세, 통신비 등 매달 고정적으로 지출되는 비용
  - **변동비 (`VARIABLE`):** 식비, 쇼핑 등 유동적인 지출 비용
- **불변식:** 성격(`nature`)은 **`EXPENSE` 일 때만 존재하며, `EXPENSE` 에는 필수**입니다.
  예산 통제의 기준이므로 지출에 성격이 없는 상태를 허용하지 않습니다. 반대로 수입·이체에는 지정할 수 없습니다.
- **타입 변경 불가:** 카테고리의 타입은 변경하지 않습니다. 이미 기록된 거래의 의미가 뒤바뀌기 때문입니다.
- **참조 무결성:** 거래나 할부 계획이 사용 중인 카테고리는 삭제할 수 없습니다 (HTTP 409).
- **분류 구조: 단일 계층(flat) 을 유지합니다.** 상위/하위 카테고리를 두지 않습니다.
  개인 가계부에서는 "식비 > 외식 / 식료품" 같은 2단 분류의 이득보다, 집계 시 롤업 규칙과
  순환 참조 방지 같은 복잡도가 더 큽니다. 세분화가 필요하면 `식비`, `외식비` 처럼
  같은 계층에서 카테고리를 나눕니다.
- **기본 제공:** 급여·기타수입·계좌이체·월세·통신비·보험료·구독료·식비·교통비·쇼핑·의료비·문화생활·경조사 13종을 시드 데이터로 제공합니다.

> 회사 전액 지원 통신비처럼 실지출이 없는 항목은 카테고리에서 빼지 않고, 해당 **거래**에
> `isExcludedFromStats` 를 지정해 집계에서 제외합니다 (2-① 참고).

### ③ 결제 수단 (PaymentMethod)
- **속성:** ID, 이름, 타입(신용카드 `CREDIT_CARD`, 체크카드 `CHECK_CARD`, 계좌 `BANK_ACCOUNT`, 현금 `CASH`), 결제일(`paymentDay`), **마감일(`closingDay`)**
- **특징:** 우리카드, 삼성카드, 현대카드 등 다양한 카드사 및 결제 수단을 등록하고, 카드별 지출 점유율 및 통계를 산출할 수 있도록 연결합니다.
- **불변식:**
  - **신용카드는 결제일이 필수**입니다. 결제일이 없으면 청구일을 산출할 수 없어 현금 흐름을 통제할 수 없습니다.
  - 결제일·마감일은 **신용카드에만** 지정할 수 있습니다. 즉시 결제되는 수단(체크카드·계좌·현금)에는 두지 않습니다.
  - 결제일·마감일은 1~31 범위입니다. 해당 월에 없는 일자는 청구일 계산 시점에 말일로 보정합니다.
- **마감일(`closingDay`):** 카드사 결제 주기의 마감 기준일입니다. **선택 항목이며, 미설정 시 말일 마감(= 익월 결제)으로 간주**합니다.
- **참조 무결성:** 거래나 할부 계획이 사용 중인 결제 수단은 삭제할 수 없습니다 (HTTP 409).
- **기본 제공:** `현금` 만 시드로 제공합니다. 신용카드는 결제일·마감일이 개인마다 달라 임의의 값을 심으면 청구일 산출이 틀어지므로 사용자가 직접 등록합니다.

### ④ 할부 계획 (InstallmentPlan)
- **속성:** ID, 총금액(`totalAmount`), 총 할부 개월 수(`installmentMonths`), 월 납부액(`monthlyAmount`), 사용처(`merchant`), 결제 수단, 카테고리, 할부 발생일(`spentDate`)
- **특징:** 가전이나 큰 수술비 등 대형 지출 발생 시 전체 금액을 한 번에 잡지 않고, 매달 청구일에 주기적으로 분할된 월 할부금 형태로 반영하여 예산 통계가 왜곡되는 것을 방지합니다.
- **회차 거래 생성:** 계획을 등록하면 개월 수만큼의 **거래(Transaction)가 각 회차 청구일로 자동 생성**됩니다. 계획 저장과 회차 생성은 **하나의 트랜잭션**으로 처리합니다. 일부 회차만 생성된 상태로 남으면 예산 통계가 어긋나기 때문입니다.
- **금액 분할 규칙:**
  - **월 납부액은 입력받지 않고 총금액과 개월 수에서 서버가 도출**합니다. 클라이언트가 계산한 값을 신뢰하면 회차 합계가 총액과 어긋날 수 있습니다.
  - 월 납부액은 몫(내림)이고, **나누어떨어지지 않은 나머지는 1회차에 가산**합니다 (카드사 관행).
  - **회차 금액의 합계는 항상 총금액과 정확히 일치합니다.** 예: 100만원 / 3개월 → `333,334 + 333,333 + 333,333`
- **불변식:**
  - 할부는 **2개월 이상 60개월 이하**입니다. 1개월은 할부가 아닌 일시불입니다.
  - 월 납부액이 1원 이상이어야 합니다. 즉 총금액은 개월 수 이상입니다.
  - 사용처는 공백이 아니며 100자 이하입니다.
- **중도 해지 정책:** 이미 출금된(정산 완료) 회차는 실제로 돈이 나간 기록이므로 **남기고**, 미정산 회차만 삭제합니다. 정산 완료 회차가 남으면 계획도 함께 남깁니다.

### ⑤ 청구일 산출 규칙

거래 등록 시 청구일을 생략하면 결제 수단의 결제 조건으로부터 서버가 산출합니다.
(카드사 사정으로 예외가 생긴 경우에만 청구일을 직접 지정할 수 있습니다.)

| 결제 수단 조건 | 청구일 |
|---|---|
| 현금 / 체크카드 / 계좌 (즉시 결제) | **소비일과 동일** |
| 신용카드 — 마감일 미설정 | 소비 월의 **익월** 결제일 |
| 신용카드 — 소비일의 일자 ≤ 마감일 | 소비 월의 **익월** 결제일 |
| 신용카드 — 소비일의 일자 > 마감일 | 소비 월의 **익익월** 결제일 (한 주기 이월) |

- **말일 보정:** 결제일이 해당 월에 존재하지 않으면 **그 달의 말일**로 보정합니다.
  예) 결제일 31일 → 2월 청구일은 28일(윤년 29일), 4·6·9·11월은 30일
- **할부 회차:** 1회차는 위 규칙을 따르고, 이후 회차는 매월 결제일에 순차 청구됩니다.
  회차마다 말일 보정을 다시 적용하므로 결제일 31일 카드는 2월 회차만 말일이 됩니다.
- 규칙상 청구일은 최소 익월 1일이므로 **항상 소비일보다 뒤**가 되며, 2-① 의 불변식을 위반하지 않습니다.

### ⑥ 예산 (Budget)

카테고리별 **월 예산**입니다. 다섯 번째 애그리거트 루트이며, 다른 애그리거트를 식별자로만
참조합니다.

| 항목 | 내용 |
|---|---|
| 구성 | 카테고리, 대상 월, 금액 |
| 단위 | **카테고리 하나의 한 달 예산은 하나뿐**입니다. 중복 등록은 DB 제약과 애플리케이션 서비스가 함께 막습니다 |
| 대상 | **지출 카테고리만** 허용합니다. 예산은 지출 통제 개념입니다 |
| 변경 | 금액만 바꿉니다. 카테고리와 대상 월은 예산의 정체성입니다 |

- **불변식: 금액은 0원보다 커야 합니다.** 0원 예산은 "예산을 두지 않음" 과 구분되지 않습니다.
- 지출 카테고리에만 둘 수 있다는 규칙은 **다른 애그리거트(카테고리)의 상태에 의존**하므로 예산
  애그리거트가 스스로 검증할 수 없습니다. 애플리케이션 서비스가 검증합니다 (2-② 참조).
- 대상 월은 영속화 시 **해당 월 1일로 정규화**해 저장합니다. 정규화를 매퍼 한 곳에서만 하므로
  같은 달을 가리키는 서로 다른 날짜가 저장될 수 없습니다.
- 실적 계산 규칙은 §3.2 에 있습니다.

---

## 3. 통계·분석 요구사항

§1 의 목적("소비 패턴을 분석하고 관리")을 직접 담당하는 영역입니다.
**모든 집계는 `isExcludedFromStats = true` 인 거래를 제외합니다.**

| 항목 | 기준일 축 | 내용 |
|---|---|---|
| 기간 요약 | 선택 | 수입·지출·이체 합계와 **수지**. 이체는 순자산 변동이 없어 수지 계산에서 제외 |
| 카테고리별 집계 | 선택 | 금액 내림차순 + 점유율. **타입 지정 필수**(생략 시 지출) |
| 결제 수단별 지출 점유율 | 선택 | **지출만** 집계. 수입·이체 제외 (2-③ 의 카드별 통계 요구) |
| 고정비 / 변동비 비중 | 선택 | **지출만** 집계 (2-② 의 성격 분리 요구) |
| 청구 예정액 | **청구일 고정** | 미정산 지출 합계. "다음 달 통장에서 얼마가 나가나" |
| 월별 추이 | 선택 | 거래가 없는 월도 0원으로 채워 그래프에 구멍이 생기지 않게 함. 최대 60개월 |
| **전월/전년 동월 대비** | 선택 | 요약(수입·지출·수지)과 카테고리별 지출의 증감액·증감률. 기준 시점은 전월 또는 작년 같은 달 |
| **이상 지출 감지** | 선택 | 최근 N개월 평균보다 크게 늘어난 지출 카테고리 목록. 기본 3개월, 최대 12개월 |
| **예산 대비 실적** | 선택 | 카테고리별 예산과 실제 지출, 소진율·잔액·상태. 예산 미설정 카테고리의 지출은 따로 합산 |
| **반복 지출 점검** | 선택 | 같은 카테고리·결제 수단·금액이 여러 달 반복된 항목과 **연간 환산액**. 기본 6개월, 최대 36개월 |
| **요일별 / 일별 패턴** | 선택 | 요일별 하루 평균과 비중, 일별 지출과 누적 |

- **카테고리별 집계는 하나의 타입만 대상으로 합니다.** 수입·지출·이체를 한 번에 집계하면
  점유율의 분모가 성질이 다른 금액을 합친 값이 되어 의미를 잃습니다.
  (수입 320만 + 지출 200만의 합계에서 각 항목이 차지하는 비율은 아무것도 말해주지 않습니다)
  타입을 생략하면 **지출**을 집계하며, 응답은 집계 대상 타입을 함께 반환해 분모가 무엇인지
  스스로 밝힙니다.
- **수지는 음수가 될 수 있습니다.** 금액(`Money`)은 0원 이상만 허용하지만 수지(`Balance`)는 별도 타입으로 분리해 적자를 표현합니다.
- **점유율은 만분율 정수로 계산**하고 표시 시점에만 백분율로 변환합니다. 부동소수점으로 누적하면 항목별 점유율의 합이 100%에서 어긋납니다.
- 집계는 **DB에서 수행**합니다. 애그리거트를 모두 읽어 메모리에서 합산하면 데이터가 늘어날수록 감당할 수 없습니다.

### 3.1 비교와 이상 감지

"이번 달 식비 45만원" 만으로는 많은지 알 수 없습니다. **무엇과 비교하느냐가 판단을 만듭니다.**
복기를 지원하려면 절대 금액이 아니라 변화를 보여줘야 합니다.

**비교 기준(`ComparisonBaseline`)**

| 값 | 기준 시점 | 쓰임 |
|---|---|---|
| `PREVIOUS_MONTH` (기본) | 직전 달 | 최근 흐름의 변화 |
| `SAME_MONTH_LAST_YEAR` | 작년 같은 달 | 계절성이 있는 지출(난방비, 명절 경조사) |

- **증감률은 기준이 0원이면 정의하지 않습니다(`null`).** 0에서 늘어난 변화의 비율은 값이 없으며,
  0% 로 표기하면 "변동 없음" 으로 정반대로 읽힙니다. 화면은 이 경우 **"신규"** 로 표시합니다.
- 증감의 **좋고 나쁨은 부호가 아니라 항목이 정합니다.** 지출 증가는 나쁘고 수입 증가는 좋습니다.
  지출만 방향이 반대이므로, 색으로 의미를 표시할 때 부호를 그대로 쓰지 않습니다.
- 카테고리별 비교는 **두 시점 카테고리의 합집합**을 대상으로 합니다. 한쪽에만 있는 카테고리는
  다른 쪽을 0원으로 두어야 "이번 달 새로 생긴 지출" 과 "이번 달 사라진 지출" 이 모두 보입니다.
- **수지는 증감률을 계산하지 않습니다.** 적자에서 흑자로 바뀐 경우처럼 부호가 뒤집히면
  비율이 의미를 갖지 못합니다. 증감액만 제공합니다.

**이상 지출 판정 기준**

기준 구간(기본 최근 3개월) 평균보다 아래 **두 조건을 모두** 만족할 때만 이상으로 봅니다.

| 조건 | 기준값 | 이유 |
|---|---|---|
| 증가율 | 30% 이상 | 평소 수준의 자연스러운 변동을 걸러냅니다 |
| 증가액 | 30,000원 이상 | 3,000원 → 6,000원(+100%)까지 알릴 필요는 없습니다 |

- 기준 평균은 **거래가 없는 월도 0원으로 포함**해 계산합니다. 그 달에 쓰지 않았다는 사실 자체가
  평소 수준의 정보이며, 기록 있는 달만 평균하면 어쩌다 한 번 쓰는 항목이 매번 이상으로 잡힙니다.
- 기준 평균이 0원인 항목(처음 생긴 지출)은 **금액 조건만으로** 판정합니다. 증가율이 없기 때문입니다.
- 판정 기준값은 응답에 함께 담아, 화면이 "무엇을 기준으로 골랐는지" 를 사용자에게 설명할 수 있게 합니다.
- 판정은 **도메인 서비스 `SpendingAnomalyDetector`** 가 담당합니다. 애플리케이션 서비스는 조회와
  전달만 합니다 (backend/CLAUDE.md 4.4).

### 3.2 예산 대비 실적

§3.1 의 비교가 **과거와의 비교**라면 예산은 **스스로 정한 기준과의 비교**입니다. 지난달보다
줄었어도 계획보다 많이 썼을 수 있습니다.

예산과 실제 지출만 있으면 나머지는 모두 파생됩니다. 파생값을 따로 저장하지 않는 이유는 서로
어긋날 수 없게 하기 위해서입니다.

| 파생값 | 정의 |
|---|---|
| 소진율 | 지출 ÷ 예산. **100% 를 넘을 수 있습니다** |
| 잔액 | 예산 − 지출. 초과하면 음수이므로 `Money` 가 아니라 `Balance` 입니다 |
| 초과 금액 | 초과하지 않았으면 0원 |
| 상태 | `WITHIN` / `WARNING` / `EXCEEDED` |

- **경고는 80% 에서 냅니다.** 100% 를 넘은 뒤에 알리면 남은 기간에 조정할 여지가 없습니다.
  기준값은 응답에 함께 담아 화면이 "80% 를 넘으면 주의" 라고 설명할 수 있게 합니다.
- **정확히 다 쓴 100% 는 초과가 아니라 경고입니다.** 초과는 넘어선 것을 뜻합니다.
- **예산이 하나도 없으면 합계 실적을 만들지 않습니다.** 0원을 분모로 한 소진율은 의미가 없고,
  "예산 없음" 과 "0% 소진" 은 다른 상태입니다. 응답에서 합계 필드가 생략됩니다.
- **지출이 0원인 카테고리도 목록에 남습니다.** "아직 하나도 안 썼다" 는 것도 실적입니다.
- **예산을 정하지 않은 카테고리의 지출은 따로 합산합니다.** 예산 항목만 보면 "예산은 다 지켰는데
  돈은 어디로 갔나" 를 설명할 수 없습니다.

### 3.3 반복 지출 점검

안 쓰는 구독을 발견하는 것은 복기의 가장 큰 수확입니다. 월 9,900원은 눈에 띄지 않지만
**연 118,800원**으로 환산하면 결정이 달라집니다. 그래서 연간 환산액을 함께 제공합니다.

**반복의 판단 조건**

| 조건 | 기준 | 이유 |
|---|---|---|
| 묶는 단위 | 같은 **카테고리 + 결제 수단 + 금액** | 구독료는 금액이 일정합니다. 매달 금액이 다른 항목은 반복 지출이 아니라 자주 쓰는 항목이며, 해지 판단의 대상이 아닙니다 |
| 등장 월 수 | 3개월 이상 | 두 달 연속은 우연일 수 있습니다. 세 달이면 패턴으로 볼 수 있습니다 |

- 등장 월 수는 **월 단위로 중복을 제거**해 셉니다. 같은 달의 두 건이 두 달치로 잡히면 반복
  판정이 부풀려집니다.
- **직전 달까지 등장했으면 "진행 중"** 으로 봅니다. 이번 달 결제일이 아직 지나지 않았을 수
  있고, 이번 달에 없다는 이유로 해지된 것처럼 보이면 판단을 오히려 방해합니다.
- **합계는 진행 중인 항목만 더합니다.** 이미 끊긴 구독을 "매달 나가는 돈" 에 넣으면 합계가
  현실과 어긋납니다.

### 3.4 시간 축 소비 패턴

카테고리별 집계는 "무엇에 썼나" 를 답하지만 "언제 쓰는가" 는 답하지 않습니다. 주말에 몰리는지,
월초에 몰리는지는 **습관**이며, 습관은 카테고리를 바꾸는 것보다 바꾸기 쉽습니다.

- **요일별은 합계가 아니라 평균으로 비교합니다.** 한 달에 어떤 요일은 다섯 번, 어떤 요일은 네 번
  옵니다. 합계만 보면 다섯 번 온 요일이 무조건 커 보이므로, **그 요일이 등장한 횟수로 나눈 값**
  으로 비교합니다. 구간이 짧아 한 번도 등장하지 않은 요일의 평균은 0원입니다.
- 일곱 요일을 모두 채워 **월요일부터** 반환합니다. 지출이 없는 요일이 빠지면 "그 요일에는 안
  쓴다" 는 사실이 화면에서 사라집니다.
- **일별은 누적을 함께 제공합니다.** 막대만으로는 큰 지출 하루에 시선이 쏠려 전체 소비 속도가
  보이지 않습니다. 거래가 없는 날도 0원으로 채워 누적선이 끊기지 않게 합니다.

---

## 4. 화면 구성 (Web Client)

모든 화면 상단에서 **소비일 기준 / 청구일 기준** 토글로 두 집계 축을 전환합니다.
두 축의 차이가 이 가계부의 핵심이므로 어떤 축으로 보고 있는지 항상 화면에 드러냅니다.

| 화면 | 내용 | 상태 |
|---|---|---|
| **대시보드** | 이번 달 수입·지출·수지(**전월 대비 증감 병기**), 다음 달 청구 예정액, **평소보다 많이 쓴 항목**, **예산 대비**, 고정비/변동비 도넛, **큰 지출 Top 5**, 최근 거래 | 구현 완료 |
| **거래 내역** | 월별·**카테고리·결제 수단 필터·메모 검색**, 건수·합계, 소비일과 청구일을 나란히 보여주는 표, 출금 완료 토글 | 구현 완료 |
| **통계·분석** | **전월/작년 같은 달 대비**(요약 + 늘어난·줄어든 지출), **언제 쓰는가**(요일별 평균 + 일별 누적), **매달 나가는 돈**(반복 지출과 연간 환산), 최근 6개월 추이(수입·지출 막대 + **수지 선**), 카테고리별·카드별 점유율 도넛, 할부 현황판 | 구현 완료 |
| **설정** | 카테고리(고정비/변동비) 목록, 결제 수단 목록·등록, **월 예산 등록·수정·삭제** | 구현 완료 |
| **거래 등록 모달** | 모든 화면 상단에서 열리는 수입·지출·이체 입력 폼 | 구현 완료 |
| **결제 수단 등록 모달** | 설정 화면에서 열리는 카드·계좌·현금 등록 폼 | 구현 완료 |

- **금액 표기:** 원화(KRW) 천 단위 구분, 자릿수 정렬을 위한 고정폭 숫자. 수지는 부호를 드러냄
- **날짜 표기:** 소비일과 청구일을 같은 행에 병기해 두 날짜의 차이를 인지하게 함

### 4.0 복기 흐름 (드릴다운)

§1 의 목적 중 "소비 생활 복기" 는 **숫자에서 그 숫자를 만든 거래로 내려가는 경로**로 지원합니다.

1. 대시보드나 통계에서 큰 금액을 발견합니다. (예: "쇼핑 1,128,000원 · 53.1%")
2. 그 항목을 누르면 해당 카테고리로 필터된 거래 목록으로 이동합니다.
   (`/transactions?categoryId=10`)
3. 목록 상단의 **건수·합계**로 통계에서 본 금액과 일치하는지 바로 확인됩니다.
4. 개별 거래의 메모·소비일·청구일을 보고 "왜 컸는지" 를 파악합니다.
   (예: "냉장고 할부 3회차가 100만원이었다")

필터는 **URL 쿼리 파라미터**에 담습니다. 링크로 전달돼야 하고 뒤로 가기·새로고침·공유가
자연히 동작해야 하기 때문입니다. 연월과 기준일 축은 화면 간 공유 상태이므로 경로에 담지
않으며, 화면을 옮겨도 보고 있던 달이 유지됩니다.

**큰 지출 Top 5** 는 복기의 출발점입니다. 거래 목록은 날짜순이라 45만원과 3천원이 같은
비중으로 보이므로, 금액순 상위 몇 건과 그 비중을 따로 보여줍니다.

**메모 검색**(`keyword`)은 카테고리로 잡히지 않는 축의 복기를 지원합니다. "회식", "정기결제"
같은 말은 여러 카테고리에 흩어져 있어 분류로는 모을 수 없습니다. 메모 부분 일치로 검색하며
`%`·`_` 같은 문자도 그대로 찾을 수 있게 이스케이프합니다. 검색어 역시 URL 에 담기므로
필터와 함께 링크로 전달됩니다.

**평소보다 많이 쓴 항목**(§3.1)은 복기의 진입점을 사용자가 찾지 않아도 되게 합니다. 도넛과
막대는 "무엇에 얼마 썼나" 를 보여주지만 "이번 달이 이상한가" 는 답하지 않습니다.

### 4.1 거래 등록 모달

수입·지출·이체를 직접 입력하는 화면입니다. 다음 원칙을 따릅니다.

- **청구일은 입력하지 않는 것이 기본입니다.** 클라이언트가 계산하지 않고 서버가 산출합니다(2-⑤).
  선택한 결제 수단에 따라 어떻게 정해지는지 폼에서 안내합니다.
  - 즉시 결제 수단: "청구일은 소비일과 같고, 출금 완료로 등록됩니다"
  - 신용카드: "매월 N일 결제 카드입니다. 마감일 M일을 넘긴 소비는 한 주기 뒤로 밀립니다"
  - 카드사 사정으로 예외가 생긴 경우에만 `청구일 직접 지정` 을 켜서 입력합니다.
- **등록 후 모달을 닫지 않고 산출된 청구일을 보여줍니다.** 금액·메모만 비우고 카테고리·결제
  수단·소비일은 유지해 연속 입력을 돕습니다. 소비일과 청구일이 어떻게 갈리는지 사용자가
  바로 확인할 수 있습니다.
- **출금 완료 체크박스는 신용카드에서만 노출합니다.** 즉시 결제 수단은 서버가 도출하므로(2-①)
  사용자가 지정할 필요가 없습니다. 체크하지 않으면 값을 전송하지 않아 서버 도출이 동작합니다.
- **도메인 규칙 위반은 서버 메시지를 그대로 표시합니다.** 클라이언트 검증은 필수값과 입력 형식
  (금액이 숫자인지, 0원보다 큰지)까지만 합니다. 청구일과 소비일의 관계 같은 규칙을 양쪽에
  두면 반드시 어긋납니다.

### 4.2 결제 수단 등록 모달

카드·체크카드·계좌·현금을 등록하는 화면입니다.

- **종류를 고르면 필요한 입력만 나타납니다.** 신용카드에만 결제일(필수)과 마감일(선택)을
  노출하고, 즉시 결제 수단에는 두 입력을 노출하지 않으며 요청에도 담지 않습니다.
  서버가 "즉시 결제되는 수단에는 결제일을 지정할 수 없습니다" 로 거부하기 때문입니다.
- **각 입력이 청구일에 어떤 영향을 주는지 함께 안내합니다.**
  - 결제일: "매월 이 날짜에 통장에서 출금됩니다. 해당 월에 없는 날짜면 말일로 보정됩니다"
  - 마감일: "마감일을 넘긴 소비는 한 청구 주기 뒤로 밀립니다. 비우면 말일 마감으로 봅니다"
- **등록 후 저장된 결제 조건을 보여줍니다.** 예: "현대카드 등록되었습니다. 결제일 25일 ·
  마감일 12일". 종류는 남기고 이름·일자만 비워 연속 등록을 돕습니다.
- 신용카드 여부 판단은 어떤 입력을 보여줄지 정하기 위한 **폼 차원의 판단**입니다. 새 결제
  수단은 아직 서버에 없어 `immediateSettlement` 를 받아올 수 없기 때문입니다.
  **판정 권한은 서버에 있습니다.**
- 기본 제공 결제 수단이 `현금` 하나뿐인 이유(2-③)와 맞물려, 사용자는 이 화면에서 자신의
  카드를 등록하는 것으로 사용을 시작합니다.

---

## 5. 시스템 아키텍처 및 모노레포 구조 (`Beetle/`)

프로젝트는 모노레포 구조로 관리되며, 백엔드 서버는 클린 아키텍처 및 엄격한 의존성 역전 원칙(DIP)을 따릅니다.

```text
Beetle/                        # 루트 디렉토리
 ┣ BEETLE_PRD.md               # 전체 프로젝트 기획 및 요구사항 정의서 (도메인 요구사항의 정본)
 ┣ CLAUDE.md                   # 루트 프로젝트(인프라/모노레포/공통 원칙) 가이드
 ┣ README.md                   # 실행 및 개발 안내
 ┣ docker-compose.yml          # 로컬 인프라 (MySQL + 백엔드 + 프론트엔드) 통합 실행
 ┣ .env.example                # 포트/비밀번호 오버라이드 예시
 ┣ backend/                    # 백엔드 서버 (Spring Boot)
 │   ┣ CLAUDE.md               # 백엔드 전용 개발·아키텍처·테스트 가이드
 │   ┣ Dockerfile              # 멀티스테이지 빌드 (JDK 빌드 -> JRE 런타임)
 │   ┣ src/main/resources/db/
 │   │   ┣ migration/          # Flyway 스키마 마이그레이션 (V1~V3)
 │   │   ┗ seed/               # 시드 데이터 (V4). 테스트에서는 적용하지 않음
 │   ┗ src/main/kotlin/com/example/beetle/
 │       ┣ domain/             # 순수 비즈니스 로직 및 도메인 모델 (Spring/JPA 독립적)
 │       │   ┣ model/          # 애그리거트 루트, 엔티티, 값 객체
 │       │   ┣ service/        # 도메인 서비스 (BillDateCalculator, InstallmentScheduler)
 │       │   ┣ exception/      # 도메인 예외
 │       │   ┣ repository/     # 리포지토리 인터페이스 = 아웃바운드 포트 (DIP)
 │       │   ┗ query/          # 통계 집계 전용 조회 포트 (Read Model)
 │       ┣ application/        # 유스케이스 및 애플리케이션 서비스
 │       ┣ infrastructure/     # Spring Data JPA 엔티티, 리포지토리, 어댑터, 집계 쿼리
 │       ┣ presentation/       # REST 컨트롤러, DTO, 전역 예외 핸들러
 │       ┗ config/             # 도메인 서비스 빈 등록, OpenAPI 설정 (composition root)
 ┗ frontend/                   # 웹 클라이언트 (React + Vite)
     ┣ CLAUDE.md               # 프론트엔드 전용 구조·상태 관리·테스트 가이드
     ┣ Dockerfile              # 멀티스테이지 빌드 (Node 빌드 -> nginx 서빙)
     ┣ nginx.conf              # SPA 폴백 + /api 백엔드 프록시
     ┗ src/
         ┣ types/              # 백엔드 DTO 와 1:1 대응하는 타입
         ┣ api/                # API 클라이언트 및 도메인별 모듈
         ┣ lib/                # 프레임워크 독립 순수 로직 (포맷터, 기간 계산)
         ┣ store/              # 전역 상태 (Zustand): 조회 기준, UI 상태
         ┣ features/           # 도메인별 기능 모듈 (쿼리 훅, 차트 변환)
         ┣ components/         # 공통 UI 컴포넌트
         ┗ pages/              # 화면 단위 컴포넌트
```

### 5.1 규칙의 강제

아키텍처와 테스트 규칙은 문서가 아니라 **테스트로 강제**합니다. 위반 시 빌드가 실패합니다.

| 위치 | 장치 | 강제하는 규칙 |
|---|---|---|
| backend | `ArchitectureTest` | 레이어 의존 방향, 도메인의 프레임워크 독립성, JPA 엔티티 노출 금지 |
| backend | `DomainModelConventionTest` | 애그리거트 루트의 `data class` 금지, 식별자 기반 동일성 |
| backend | `MockKValueClassConventionTest` | 값 객체 파라미터에 `any()` 매처 금지 |
| backend | JaCoCo 커버리지 검증 | 도메인 레이어 분기 커버리지 90% 이상 |
| frontend | Vitest `thresholds` | `lib/`·`store/` 순수 로직 커버리지 90% 이상 |
| frontend | ESLint `no-explicit-any` | `any` 배제 |

---

## 6. 인프라 및 실행 환경
- **로컬 개발 환경:** MySQL, Spring Boot 앱, 웹 클라이언트를 모두 로컬 Docker 컨테이너 (`docker-compose.yml`)로 구성하여 구동
- **환경 분리:** 로컬(dev)과 배포(prod)를 나눠 관리하며 **로컬이 기본값**입니다. 프로필이나
  파일 지정을 빠뜨렸을 때 운영 설정이 아니라 로컬 설정으로 뜨는 방향이 안전합니다.

  | 영역 | 로컬(dev) | 배포(prod) |
  |---|---|---|
  | 백엔드 | `application-dev.yml` (미지정 시 기본) | `application-prod.yml` (`SPRING_PROFILES_ACTIVE=prod`) |
  | 프론트엔드 | `.env.development` | `.env.production` |
  | 컨테이너 | `docker-compose.override.yml` (자동 적용) | `docker-compose.prod.yml` (`-f` 로 명시) |

  배포 환경은 **접속 정보에 기본값을 두지 않아 없으면 기동에 실패**하고, **시드 데이터를
  적용하지 않으며**, API 문서와 관리 엔드포인트(health 제외)를 차단하고, 프론트엔드 포트만
  호스트에 노출합니다. 이 정책은 `ProfileConfigurationTest` 가 설정 파일을 읽어 검증합니다
- **데이터 영속성:** Docker Volume을 활용하여 MySQL 데이터 유실 방지
- **스키마 관리:** Flyway 로만 관리하며 Hibernate 는 `ddl-auto=validate` 로 검증만 수행합니다. 스키마(`db/migration`)와 시드 데이터(`db/seed`)를 분리해, 빈 DB 를 전제로 하는 테스트에는 시드를 적용하지 않습니다.
- **통합 테스트:** H2 대신 **Testcontainers 로 실제 MySQL** 을 사용합니다. 방언 차이로 인한 거짓 통과·거짓 실패를 막기 위함입니다.
- **오리진 통일:** 프론트엔드 컨테이너의 nginx 가 `/api` 를 백엔드로 프록시합니다. 브라우저가 보는 오리진이 하나로 유지되므로 CORS 설정이 필요하지 않습니다. 개발 시에는 Vite 개발 서버가 같은 역할을 합니다.
- **접속 주소:** 로컬은 프론트엔드 `:5173`, 백엔드 `:8080`, MySQL `:13306` (`.env` 로 변경 가능).
  MySQL 만 표준 포트를 피하는 이유는 호스트에 이미 MySQL 이 있는 환경에서 충돌하기 때문이며,
  컨테이너 안쪽 포트는 3306 그대로입니다. 배포는 프론트엔드만 노출합니다(기본 `:80`)
- **API 문서:** springdoc-openapi 로 `/swagger-ui.html`, `/v3/api-docs` 제공 (**로컬 전용**)
- **AI 협업 가이드 (`CLAUDE.md`):**
  - 루트의 `CLAUDE.md` 는 모노레포 전반, 인프라 실행, 전 영역 공통 개발 원칙을 담당합니다.
  - `backend/CLAUDE.md` 는 아키텍처 규칙, 패키지 구조, DDD 전술 설계, 코틀린 컨벤션, 테스트 정책을 정의합니다.
  - `frontend/CLAUDE.md` 는 프론트엔드 구조, 상태 관리 분리, 테스트 정책을 정의합니다.
  - 세 문서 모두 본 PRD(`BEETLE_PRD.md`)의 도메인 요구사항을 기반으로 구현하도록 연동됩니다.

---

## 7. API 개요

| 메서드 | 경로 | 설명 |
|---|---|---|
| `POST` / `GET` | `/api/categories` | 카테고리 등록 / 목록 (타입 필터) |
| `GET` / `PATCH` / `DELETE` | `/api/categories/{id}` | 카테고리 단건 조회 / 수정 / 삭제 |
| `POST` / `GET` | `/api/payment-methods` | 결제 수단 등록 / 목록 |
| `GET` / `PATCH` / `DELETE` | `/api/payment-methods/{id}` | 결제 수단 단건 조회 / 수정 / 삭제 |
| `POST` | `/api/transactions` | 거래 등록 (청구일 자동 산출) |
| `GET` | `/api/transactions?basis=&from=&to=` | 기간 조회 (기준일 축 전환, 카테고리·결제 수단 필터, `keyword` 메모 검색) |
| `GET` / `PATCH` / `DELETE` | `/api/transactions/{id}` | 거래 단건 조회 / 수정 / 삭제 |
| `POST` / `DELETE` | `/api/transactions/{id}/settlement` | 출금 완료 표시 / 되돌리기 |
| `PATCH` | `/api/transactions/{id}/stats-exclusion` | 통계 집계 제외 여부 변경 |
| `POST` / `GET` | `/api/installment-plans` | 할부 등록 (회차 거래 자동 생성) / 목록 |
| `GET` / `DELETE` | `/api/installment-plans/{id}` | 할부 상세(회차 포함) / 중도 해지 |
| `GET` | `/api/statistics/summary` | 기간 요약 |
| `GET` | `/api/statistics/categories` | 카테고리별 집계와 점유율 (`type` 기본값 `EXPENSE`) |
| `GET` | `/api/statistics/payment-methods` | 카드별 지출 점유율 |
| `GET` | `/api/statistics/expense-nature` | 고정비 / 변동비 비중 |
| `GET` | `/api/statistics/upcoming-bills?month=` | 청구 예정액 |
| `GET` | `/api/statistics/monthly-trend` | 월별 추이 |
| `GET` | `/api/statistics/month-comparison?month=&baseline=` | 전월/전년 동월 대비 증감 (§3.1) |
| `GET` | `/api/statistics/category-anomalies?month=&baselineMonths=` | 이상 지출 감지 (§3.1) |
| `GET` | `/api/statistics/recurring-expenses?month=&windowMonths=` | 반복 지출 점검 (§3.3) |
| `GET` | `/api/statistics/spending-pattern?from=&to=` | 요일별·일별 소비 패턴 (§3.4) |
| `POST` / `GET` | `/api/budgets?month=` | 예산 등록 / 대상 월의 목록 (§2-⑥) |
| `GET` | `/api/budgets/performance?basis=&month=` | 예산 대비 실적 (§3.2) |
| `GET` / `PATCH` / `DELETE` | `/api/budgets/{id}` | 예산 단건 조회 / 금액 수정 / 삭제 |

### 7.1 오류 응답 규약

모든 오류는 `code` 와 `message` 를 가진 동일한 형태로 응답합니다. 클라이언트는 `code` 로 분기합니다.

| `code` | HTTP | 의미 |
|---|---|---|
| `VALIDATION_FAILED` | 400 | 요청 필드 검증 실패 (`fieldErrors` 포함) |
| `INVARIANT_VIOLATION` | 400 | 도메인 불변식 위반 |
| `MISSING_PARAMETER` / `INVALID_PARAMETER` / `MALFORMED_REQUEST` | 400 | 요청 형식 오류 |
| `RESOURCE_NOT_FOUND` | 404 | 대상 없음 |
| `DOMAIN_STATE_CONFLICT` | 409 | 현재 상태에서 허용되지 않는 요청 (이름 중복, 이미 정산된 거래, 참조 중인 마스터 데이터 삭제 등) |
| `INTERNAL_ERROR` | 500 | 처리되지 않은 서버 오류 |

---

## 8. 구현 범위와 향후 과제

### 8.1 구현 완료
- §2 의 다섯 개 도메인(①~④ + ⑥ 예산), §2-⑤ 청구일 산출, §3 통계 전 항목
  (§3.1 비교·이상 감지, §3.2 예산 실적, §3.3 반복 지출, §3.4 시간 축 패턴 포함), §7 의 모든 API
- §4 의 네 개 화면과 거래 등록 모달(4.1), 결제 수단 등록 모달(4.2)
- §4.0 복기 흐름: 드릴다운 링크, 큰 지출 Top 5, 메모 검색, 전월 대비 증감, 평소보다 많이 쓴 항목,
  예산 대비 실적, 반복 지출 점검, 요일별·일별 패턴
- Docker 기반 로컬 실행 환경, Flyway 마이그레이션, API 문서

### 8.2 결정 대기 (요구사항 미정의)

아래는 현재 요구사항이 정의되지 않은 항목입니다. 구현은 임의의 기본 동작을 따르고 있으므로,
결정이 필요해지는 시점에 본 문서에 규칙을 추가합니다.

**도메인 모델이 표현할 수 없는 것 (구조 변경 필요)**

| 항목 | 현재 한계 |
|---|---|
| 이체의 양쪽 계좌 | `Transaction` 은 결제 수단을 하나만 가집니다. "신한계좌 → 저축계좌" 의 출금·입금 양쪽을 표현할 수 없어 `TRANSFER` 는 반쪽만 기록됩니다 |
| 환불 / 결제 취소 | `amount > 0` 불변식 때문에 음수를 기록할 수 없습니다. 거래 삭제만 가능하므로 취소 이력이 남지 않습니다 |
| 할부 수수료 / 이자 | 총액을 개월 수로만 나눕니다. 무이자 할부를 가정한 모델입니다 |
| 다중 통화 | KRW 정수 고정입니다. 해외 결제는 원화 환산액만 기록합니다 |

**운영 규칙**

| 항목 | 현재 동작 |
|---|---|
| 할부 회차 거래의 개별 수정 | 카테고리·금액 변경이 열려 있어, 회차를 수정하면 계획과 어긋나거나 "회차 합계 == 총액" 성질이 깨질 수 있습니다 |
| 정산 표시의 시점 | 등록 시 도출(2-①) 이후에는 수동입니다. 청구일 경과 시 자동 정산은 하지 않습니다 |
| 고정비의 반복 등록 | 자동 생성이 없어 매달 수동 입력해야 합니다 |
| 소비일의 허용 범위 | 제한이 없습니다. 먼 미래·먼 과거 날짜도 등록됩니다 |
| 마감일과 결제일의 관계 | 임의 조합을 허용합니다 (마감일 25일 + 결제일 14일 등) |
| 가계부 "월" 의 정의 | 1일~말일 고정입니다. 급여일 기준 월(예: 25일~24일)은 지원하지 않습니다 |
| 삭제 정책 | 물리 삭제입니다. 실수로 지운 거래는 복구할 수 없습니다 |
| 할부 계획 수정 | 등록·해지만 있고 수정 API 가 없습니다 |

### 8.3 미구현 (향후 과제)
- **남은 입력 폼 UI:** 할부 등록 폼, 카테고리 등록·수정 화면, 결제 수단 수정·삭제 UI,
  거래 수정·삭제 UI. API 계층과 변경(mutation) 훅은 모두 구현되어 있으므로 폼 컴포넌트만
  추가하면 연결됩니다. (거래 등록은 4.1, 결제 수단 등록은 4.2 로 구현 완료)
- **인증 / 멀티 사용자:** 본 시스템은 "프라이빗 가계부" 전제이므로 **단일 사용자**를 가정하고 인증을 구현하지 않았습니다.
  엔티티에 `userId` 를 미리 두지 않았으므로, 필요해지면 마이그레이션으로 추가합니다.
- **거래 내역 가져오기:** 카드사 명세서나 엑셀 파일 일괄 등록.
- **예산의 반복 설정:** 매달 예산을 다시 입력해야 합니다. "지난달 예산 복사" 나 기본 예산이 없습니다.
- **예산 소진 속도:** 월 진행률(며칠이 지났는지) 대비 소진율을 비교하면 월중에도 판단할 수 있습니다.
  현재는 월말 기준의 소진율만 제공합니다.
