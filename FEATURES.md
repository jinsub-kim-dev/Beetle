# 기능 안내

Beetle 이 **무엇을 할 수 있는가**를 정리한 문서다. 화면에서 하는 일을 기준으로 적었다.

| 문서 | 역할 |
|---|---|
| 본 문서 | 제공하는 기능 전체와 사용 방법 |
| [BEETLE_PRD.md](BEETLE_PRD.md) | **도메인 규칙의 정본.** 왜 그렇게 동작하는지, 경계 조건은 무엇인지 |
| [SETUP.md](SETUP.md) · [README.md](README.md) | 환경 구축과 실행·운영 |

규칙의 근거가 필요하면 각 절 끝의 `PRD n` 표시를 따라간다. 본 문서와 PRD 가 어긋나면
**PRD 가 옳다.**

---

## 0. 이 가계부가 다른 점

### 소비일과 청구일을 나눠 기록한다

카드를 긁은 날(**소비일**, `spentDate`)과 통장에서 돈이 빠지는 날(**청구일**, `billDate`)은
다르다. 둘을 따로 저장하고, 모든 조회·통계를 **두 축 중 하나로** 볼 수 있다.

| 기준일 축 | 답하는 질문 | 쓰는 곳 |
|---|---|---|
| **소비일 기준** | "이번 달에 얼마를 **썼나**" | 소비 패턴 분석 |
| **청구일 기준** | "이번 달에 통장에서 얼마가 **나가나**" | 현금 흐름 통제 |

화면 맨 위의 토글로 언제든 바꿀 수 있고, 바꾼 축은 모든 화면에서 유지된다.

**청구일은 직접 입력하지 않는다.** 결제 수단의 결제일·마감일에서 서버가 산출한다.
결제일 31일 카드의 2월 청구일은 말일(윤년이면 29일)로 보정되고, 마감일을 넘긴 소비는 한
청구 주기 뒤로 밀린다. (PRD 2-⑤)

### 고정비와 변동비를 나눈다

지출 카테고리는 **고정비**(월세·통신비 등)나 **변동비**(식비·쇼핑 등) 중 하나를 반드시
갖는다. 줄일 수 있는 지출과 줄일 수 없는 지출을 분리해야 통제할 수 있기 때문이다.
(PRD 2-②)

### 할부를 회차로 쪼갠다

100만원짜리 냉장고를 한 달에 다 잡으면 그 달 통계가 망가진다. 할부를 등록하면 회차별
거래가 각 회차 청구일로 자동 생성된다. (PRD 2-④)

### 기록은 남기되 집계에서 빼는 항목이 있다

회사가 전액 지원하는 통신비처럼 실지출이 없는 항목은 **거래 단위**로 집계에서 제외한다.
카테고리 단위가 아닌 이유는 같은 카테고리에도 자부담분이 섞이기 때문이다. (PRD 2-①)

---

## 1. 기록하기

### 1.1 거래 등록

**어디서:** 모든 화면 오른쪽 위 `거래 등록` 버튼

수입·지출·이체를 입력한다. 입력하는 것은 카테고리, 결제 수단, 금액, 소비일, 메모다.

- **청구일은 비워 둔다.** 결제 수단에 따라 서버가 정한다. 카드사 사정으로 예외가 생긴
  경우에만 직접 지정한다
- **출금 완료 여부도 자동으로 정해진다.** 현금·체크카드·계좌는 쓰는 순간 나간 돈이므로
  완료, 신용카드는 청구일에 나가므로 미완료다
- 통계에서 빼려면 `통계 집계 제외` 를 켠다
- 등록해도 창이 닫히지 않는다. **산출된 청구일을 보여주고 금액·메모만 비워** 연속 입력을
  돕는다 (PRD 4.1)

### 1.2 거래 수정·삭제

**어디서:** 거래 내역 화면의 행 오른쪽 연필 버튼

카테고리·금액·소비일·메모를 고친다. 삭제는 금액을 보여주는 확인 단계를 거친다.

- **결제 수단은 바꿀 수 없다.** 청구일 산출의 근거이므로, 바꾸려면 지우고 다시 등록한다
- 소비일을 바꾸면 **청구일은 서버가 다시 산출**한다
- 잠기는 경우가 둘 있고, 화면이 이유를 알려 준다
  - **할부 회차**: 금액·일자 변경과 개별 삭제 불가. 계획을 해지하고 다시 등록해야 한다
    (회차 금액을 바꾸면 "회차 합계 == 총액" 이 깨진다)
  - **출금 완료된 거래**: 금액·일자 변경 불가. 먼저 출금 완료를 되돌린다

(PRD 4.3, 2-④)

### 1.3 출금 완료 표시

**어디서:** 거래 내역 화면의 `출금` 열 버튼

통장에서 실제로 나갔는지 표시한다. 누르면 완료/미완료가 토글된다. 미완료 지출의 합이
대시보드의 **다음 달 청구 예정액**이 된다.

### 1.4 고정비 이월

**어디서:** 거래 내역 화면 오른쪽 위 `고정비 이월` 버튼

월세·통신비·구독료처럼 매달 같은 금액으로 반복되는 지출을 **직전 달에서 복사**한다.
기록이 끊기면 모든 분석이 무의미해지므로, 손으로 다시 입력하는 부담을 없앤다.

- 대상은 **고정비 카테고리의 지출만**이다. 변동비는 매달 금액이 다르므로 제외된다
- **여러 번 눌러도 중복이 생기지 않는다.** 이미 있으면 건너뛰고 그 개수를 알려 준다
- 소비일은 같은 일자로 옮기고, 없는 일자는 말일로 맞춘다
- 청구일은 새 소비일과 결제 조건으로 **다시 산출**한다
- 할부 회차는 계획이 이미 만들어 두었으므로 제외된다

> 자동 생성(스케줄러)이 아니라 버튼인 이유: 이 시스템은 항상 켜져 있지 않다. 예정된 날에
> 꺼져 있으면 생성이 조용히 누락된다. (PRD 2-⑦)

### 1.5 할부 등록·해지

**어디서:** 통계·분석 화면의 `할부 현황` 카드

총 금액·개월 수·사용처를 입력하면 **회차 수만큼의 거래가 각 회차 청구일로 생성**된다.

- 월 납부액은 서버가 계산한다. 나누어떨어지지 않는 나머지는 1회차에 가산되어 **회차 합계가
  총액과 정확히 일치**한다
- 등록 직후 **만들어진 회차(번호·청구일·금액)를 그대로 보여준다**
- 해지하면 **미정산 회차만 지운다.** 이미 출금된 회차는 남긴다 — 그 돈은 실제로 나갔으므로
  기록을 지우면 과거 통계가 어긋난다. 정산된 회차가 남으면 계획도 남는다

(PRD 4.4, 2-④)

### 1.6 카테고리 관리

**어디서:** 설정 화면의 `카테고리` 카드

등록·수정·삭제한다. 항목을 누르면 수정, 오른쪽 `×` 로 삭제한다.

- 지출을 고르면 **고정비/변동비 입력이 나타난다**(필수). 수입·이체에는 지정할 수 없다
- **종류(수입/지출/이체)는 수정할 수 없다.** 이미 기록된 거래의 의미가 뒤바뀐다
- 그 카테고리를 쓰는 거래나 할부가 있으면 **삭제가 거부된다**
- 계층 없는 단일 분류다. 상위/하위 카테고리를 두지 않는다 (PRD 2-②)

처음 실행하면 기본 카테고리 13종이 들어 있다(급여·기타수입·계좌이체 + 고정비 4종 +
변동비 6종). 배포 환경은 빈 상태로 시작한다.

### 1.7 결제 수단 관리

**어디서:** 설정 화면의 `결제 수단` 카드

신용카드·체크카드·계좌·현금을 등록·수정·삭제한다.

- **신용카드에만 결제일(필수)·마감일(선택)** 이 나타난다. 결제일이 없으면 청구일을 산출할
  수 없다. 마감일 미설정은 "익월 결제" 를 뜻한다
- 즉시 결제 수단(체크카드·계좌·현금)은 청구일이 소비일과 같고 출금 완료로 기록된다
- **종류는 수정할 수 없다.** 청구일 산출 방식이 바뀌면 이미 기록된 거래의 청구일이
  설명되지 않는다
- 사용 중인 결제 수단은 삭제가 거부된다

처음 실행하면 `현금` 하나만 있다. 카드는 결제일·마감일이 개인마다 달라 직접 등록한다.
(PRD 4.2, 2-③)

### 1.8 예산 설정

**어디서:** 설정 화면의 `<대상 월> 예산` 카드 (예: `2026년 9월 예산`)

카테고리별 **월 예산**을 정한다. 화면 위에서 고른 달이 대상이다.

- 지출 카테고리만 대상이고, 한 카테고리의 한 달 예산은 하나뿐이다
- 0원은 둘 수 없다. "예산 없음" 과 구분되지 않기 때문이다
- 이미 예산이 있는 카테고리는 선택 후보에서 빠진다
- 금액만 수정한다. 카테고리와 달은 예산의 정체성이다

(PRD 2-⑥)

---

## 2. 보기 — 대시보드

**어디서:** `대시보드` 탭. 이번 달을 한눈에 본다.

| 영역 | 보여주는 것 |
|---|---|
| **수입 / 지출 / 수지** | 선택한 달의 합계. 각각 **전월 대비 증감**을 함께 표시 |
| **다음 달 청구 예정** | 아직 출금되지 않은 금액. "통장에서 얼마가 나가나" |
| **평소보다 많이 쓴 항목** | 최근 3개월 평균보다 튄 카테고리 (2.1절) |
| **예산 대비** | 카테고리별 진행 바와 전체 소진율 (2.2절) |
| **고정비 / 변동비 비중** | 도넛 차트 |
| **큰 지출 Top 5** | 금액순 상위 5건과 그 비중 |
| **최근 거래** | 선택한 축 기준 최근 목록 |

- **수지는 음수가 될 수 있다.** 이체는 순자산 변동이 없으므로 수지 계산에서 빠진다
- 증감의 색은 **부호가 아니라 의미**를 따른다. 지출이 준 것은 좋은 일이므로 초록이다
- 기준이 0원이라 증감률을 낼 수 없으면 `신규` 로 표시한다. 0% 로 쓰면 "변동 없음" 으로
  반대로 읽힌다

### 2.1 평소보다 많이 쓴 항목

최근 3개월 평균보다 **30% 이상 그리고 30,000원 이상** 늘어난 지출 카테고리를 찾아 준다.
두 조건을 모두 요구하는 이유는 3,000원 → 6,000원(+100%)까지 알릴 필요가 없기 때문이다.

- 기준 평균은 **거래가 없던 달도 0원으로 포함**해 계산한다. 안 썼다는 것도 정보다
- 판정 기준을 화면에 함께 적어, 무엇을 근거로 골랐는지 보이게 한다
- 항목을 누르면 그 카테고리의 거래 목록으로 간다

(PRD 3.1)

### 2.2 예산 대비

예산을 정한 카테고리마다 **얼마 중 얼마를 썼는지** 진행 바로 보여준다.

| 상태 | 기준 |
|---|---|
| 여유 | 소진율 80% 미만 |
| 주의 | 80% 이상 |
| 초과 | 예산을 넘김 (정확히 100% 는 주의) |

- 경고를 80% 에서 내는 이유: 100% 를 넘은 뒤 알리면 조정할 여지가 없다
- 지출이 0원인 카테고리도 목록에 남는다. "아직 안 썼다" 도 실적이다
- **예산을 정하지 않은 카테고리의 지출은 따로 합산**해 아래에 보여준다. 예산 항목만 보면
  "예산은 지켰는데 돈은 어디로 갔나" 를 알 수 없다

(PRD 3.2)

---

## 3. 분석 — 통계·분석

**어디서:** `통계·분석` 탭

### 3.1 전월 / 작년 같은 달 대비

수입·지출·수지의 증감과, **늘어난 지출·줄어든 지출** 카테고리를 각각 상위 5개까지
보여준다. 비교 기준은 버튼으로 바꾼다.

- **전월** — 최근 흐름의 변화
- **작년 같은 달** — 난방비·명절 경조사처럼 계절성이 있는 지출

한쪽 달에만 있는 카테고리도 반대쪽을 0원으로 채워 포함한다. 새로 생긴 지출과 사라진
지출이 모두 보여야 하기 때문이다. 각 항목은 거래 목록으로 가는 링크다. (PRD 3.1)

### 3.2 언제 쓰는가

"무엇에 썼나" 를 넘어 **"언제 쓰는가"** 를 본다. 습관은 카테고리보다 바꾸기 쉽다.

- **요일별 하루 평균** 막대 — 합계가 아니라 평균이다. 한 달에 어떤 요일은 다섯 번, 어떤
  요일은 네 번 오므로 합계로는 비교가 성립하지 않는다
- **일별 지출과 누적** — 막대만 보면 큰 지출 하루에 시선이 쏠려 전체 속도가 안 보인다
- 가장 많이 쓴 요일과 주말 비중을 **문장으로 먼저** 알려 준다

(PRD 3.4)

### 3.3 매달 나가는 돈 (구독 점검)

같은 **카테고리 + 결제 수단 + 금액**이 최근 6개월 중 3개월 이상 반복된 항목을 모은다.
안 쓰는 구독을 발견하는 것이 복기의 가장 큰 수확이다.

- **연간 환산액**을 함께 보여준다. 월 9,900원은 눈에 띄지 않지만 연 118,800원이면 결정이
  달라진다
- 금액을 조건에 넣는 이유: 고정비의 정체성은 "매달 같은 금액" 이다. 금액이 바뀌었다면
  (요금 인상) 확인해야 할 변화다
- 진행 중인 항목과 **최근 끊긴 항목**을 나눠 보여주고, 합계는 진행 중인 것만 더한다

(PRD 3.3)

### 3.4 최근 6개월 추이

수입·지출 막대에 **수지 선**을 겹쳐 그린다. 0선을 그어 적자 구간이 눈에 띄게 한다.
거래가 없는 달도 0원으로 채워 그래프에 구멍이 생기지 않게 한다.

### 3.5 점유율

**카테고리별 지출 점유율**과 **카드별 지출 점유율**을 도넛으로 보여준다.

- 지출만 집계한다. 수입과 지출을 섞으면 점유율의 분모가 의미를 잃는다
- 항목을 누르면 그 조건으로 필터된 거래 목록으로 간다

### 3.6 할부 현황

진행 중인 할부 계획과 총액·개월 수·월 납부액. 여기서 등록하고 해지한다(1.5절).

---

## 4. 찾기 — 거래 내역

**어디서:** `거래 내역` 탭

선택한 달의 거래를 **소비일·청구일을 나란히** 보여주는 표다. 위에 **건수와 합계**가 있어
통계에서 본 금액과 맞는지 바로 확인된다.

**필터 세 가지**

| 필터 | 쓰임 |
|---|---|
| 카테고리 | 통계에서 "식비 45만원" 을 눌러 들어오는 경로 |
| 결제 수단 | 카드별 점유율에서 들어오는 경로 |
| **메모 검색** | 카테고리로 묶이지 않는 축 — "회식", "정기결제" 등 |

필터는 **주소(URL)에 담긴다.** 그래서 통계 화면에서 링크로 넘어올 수 있고, 뒤로 가기와
새로고침·공유가 자연히 동작한다.

### 복기 흐름

이 가계부의 핵심 사용법이다.

1. 대시보드나 통계에서 큰 금액을 발견한다 — "쇼핑 1,128,000원 · 53.1%"
2. 그 항목을 누르면 **그 카테고리로 필터된 거래 목록**으로 간다
3. 목록 위의 건수·합계로 통계에서 본 금액과 일치하는지 확인한다
4. 개별 거래의 메모·소비일·청구일을 보고 "왜 컸는지" 를 파악한다 — "냉장고 할부 3회차"

(PRD 4.0)

---

## 5. 화면별 요약

| 화면 | 할 수 있는 일 |
|---|---|
| **대시보드** | 이번 달 요약(전월 대비), 청구 예정액, 이상 지출, 예산 대비, 고정비/변동비 비중, 큰 지출 Top 5, 최근 거래 |
| **거래 내역** | 조회·필터·메모 검색, 건수·합계, 출금 완료 토글, **거래 수정·삭제**, **고정비 이월** |
| **통계·분석** | 전월/작년 대비, 언제 쓰는가, 매달 나가는 돈, 6개월 추이, 카테고리·카드별 점유율, **할부 등록·해지** |
| **설정** | **카테고리**·**결제 수단**·**월 예산** 등록·수정·삭제 |
| 모든 화면 상단 | 조회 달 이동, 소비일/청구일 축 전환, **거래 등록** |

로컬에서 띄우면 화면 왼쪽 위에 `LOCAL` 배지가 보인다. 로컬 화면을 실제 가계부로 착각하고
데이터를 넣는 일을 막기 위한 것이다.

가계부 화면과 별개로 **서버 상태 페이지**가 하나 더 있다. 가계부 데이터와는 무관하고,
서버가 살아 있는지와 자원을 얼마나 쓰는지만 보여 준다. 올리지 않아도 가계부는 그대로
동작한다.

| 화면 | 할 수 있는 일 |
|---|---|
| **상태 페이지** (`:3000`) | 서버 2대와 백엔드·프론트엔드의 정상/장애, 10초 주기 CPU·메모리, 최근 장애 이력 |

절차는 [SETUP.md 5부](SETUP.md#5부-모니터링--상태-페이지) 에 있다.

---

## 6. API

화면 없이 직접 쓸 수도 있다. 전체 **39개 엔드포인트**이며, 로컬에서는
`http://localhost:8080/swagger-ui.html` 에서 스펙을 확인할 수 있다(배포 환경은 차단).

| 영역 | 엔드포인트 |
|---|---|
| 거래 | `POST`·`GET` `/api/transactions`, `GET`·`PATCH`·`DELETE` `/api/transactions/{id}`, `POST`·`DELETE` `/api/transactions/{id}/settlement`, `PATCH` `/api/transactions/{id}/stats-exclusion`, `POST` `/api/transactions/fixed-expense-carry-over` |
| 카테고리 | `POST`·`GET` `/api/categories`, `GET`·`PATCH`·`DELETE` `/api/categories/{id}` |
| 결제 수단 | `POST`·`GET` `/api/payment-methods`, `GET`·`PATCH`·`DELETE` `/api/payment-methods/{id}` |
| 할부 | `POST`·`GET` `/api/installment-plans`, `GET`·`DELETE` `/api/installment-plans/{id}` |
| 예산 | `POST`·`GET` `/api/budgets`, `GET` `/api/budgets/performance`, `GET`·`PATCH`·`DELETE` `/api/budgets/{id}` |
| 통계 | `/api/statistics` 의 `summary`, `categories`, `payment-methods`, `expense-nature`, `upcoming-bills`, `monthly-trend`, `month-comparison`, `category-anomalies`, `recurring-expenses`, `spending-pattern` |

대부분의 조회는 `basis`(`SPENT`/`BILL`)로 기준일 축을 바꾼다. 지정하지 않으면 소비일
기준이다. 오류는 `code` 와 `message` 를 가진 같은 형태로 온다. (PRD 7, 7.1)

---

## 7. 아직 없는 것

| 항목 | 현재 |
|---|---|
| **로그인·다중 사용자** | 없다. 단일 사용자를 가정했다. 외부에 노출하려면 앞단에 인증을 두어야 한다([SETUP.md](SETUP.md) 6부) |
| **환불·결제 취소** | 음수 금액을 기록할 수 없다. 거래를 지우는 방법뿐이라 취소 이력이 남지 않는다 |
| **이체의 양쪽 계좌** | 거래가 결제 수단을 하나만 가지므로 "A계좌 → B계좌" 의 반쪽만 기록된다 |
| **할부 이자·수수료** | 총액을 개월 수로 나눌 뿐이다. 무이자 할부를 가정한다 |
| **다중 통화** | 원(KRW) 정수 고정이다. 해외 결제는 원화 환산액으로 기록한다 |
| **명세서 가져오기** | 카드사 명세서나 엑셀 일괄 등록이 없다. 전부 직접 입력하거나 고정비 이월을 쓴다 |
| **예산의 반복 설정** | 매달 다시 정한다. "지난달 예산 복사" 가 없다 |
| **예산 소진 속도** | 월 진행률 대비 소진율을 보여주지 않는다. 월말 기준 소진율만 있다 |
| **할부 계획 수정** | 등록과 해지만 있다. 고치려면 해지하고 다시 등록한다 |
| **자동 정산 표시** | 청구일이 지나도 자동으로 출금 완료가 되지 않는다. 직접 표시한다 |
| **삭제 복구** | 물리 삭제다. 지운 거래는 되돌릴 수 없다 |
| **장애 알림** | 상태 페이지는 보는 것만 한다. 서버가 멈춰도 알려 주지 않으므로 화면을 열어 봐야 안다 ([SETUP.md](SETUP.md) 5.6절) |

정하지 않은 운영 규칙과 구조적 한계는 [BEETLE_PRD.md](BEETLE_PRD.md) 8.2·8.3 에 있다.
