# prod(배포) 모드 기본값. `npm run build` 가 읽는다.
#
# 이 파일은 커밋된다. 비밀 값을 넣지 않는다. 프론트엔드 빌드 결과물은 브라우저로
# 그대로 내려가므로 여기에 넣은 값은 공개된다고 보아야 한다.

VITE_APP_ENV=prod

# 배포 환경은 nginx 가 같은 오리진에서 /api 를 백엔드로 프록시한다.
# 프론트엔드와 백엔드를 다른 오리진에 두는 경우에만 절대 주소를 지정한다.
VITE_API_BASE_URL=
