# 이 프로젝트는 공개 npm 레지스트리를 사용한다.
#
# 전역 ~/.npmrc 가 회사 사설 레지스트리(AWS CodeArtifact)를 가리키는 환경에서도
# 개인 프로젝트의 의존성이 사내 레지스트리를 거치지 않도록 명시한다.
# (CodeArtifact 토큰은 만료되므로 전역 설정에만 의존하면 설치가 실패한다)
registry=https://registry.npmjs.org/
